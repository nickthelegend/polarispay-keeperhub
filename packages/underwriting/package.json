{
  "name": "@polarispay/underwriting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Underwrite a borrower from their on-chain history: wallet age, DeFi tenure, Aave repayment record, liquidation history and sybil clustering.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --experimental-strip-types test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
